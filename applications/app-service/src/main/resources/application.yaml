server:
  port: 8080
spring:
  application:
    name: "RetoBC"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
adapter:
  saldos:
    url: "https://getsandbox.com/sandboxes/rough-glitter-548/balance"
  movimientos:
    url: "http://localhost:8080/dummy"
